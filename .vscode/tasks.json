{
	"version": "2.0.0",
	"tasks": [
    {
      "label": "Prepare app data folder",
      "type": "shell",
      "command": "mkdir -p _data",
    },
    {
      "label": "Shutdown debug stack",
      "type": "shell",
      "command": "docker",
      "args": ["compose", "-f", "docker-compose.debug.yml", "down", "-v"],
      "options": {
        "cwd": "${workspaceFolder}/debug"
      }
    },
    {
      "label": "Run debug stack",
      "type": "shell",
      "dependsOn": "Prepare app data folder",
      "command": "docker",
      // "dependsOn": "shutdown debug stack",
      "args": ["compose", "-f", "docker-compose.debug.yml", "up", "-d", "--wait"],
      "options": {
        "cwd": "${workspaceFolder}/debug"
      }
    },
  ]
}